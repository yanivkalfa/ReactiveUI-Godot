@class_name DemoTodoRow

component DemoTodoRow(item, onRemove) {
	return (
		<HBox style={ {"separation": 8} }>
			<Label text={ "•  " + str(item["text"]) } style={ {"expand_h": true} } />
			<Button text=" ✕ " onClick={ func(): onRemove.call(item["id"]) } />
		</HBox>
	)
}
