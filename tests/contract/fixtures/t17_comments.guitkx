component Comments {
  return (
    // leading note
    <vbox>
      /* block
			   note */
      <label {/* attr note */} text="a" />
      <!-- html-style -->
      @if (true) {
        return ( // inside a body
				<label text="b" /> )
      }
    </vbox>
  )
}
