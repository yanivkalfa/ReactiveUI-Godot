component LambdaGuard(items: Array = []) {
  var pick = func(i):
    return (i * 2)
  var total = 0
  return (
    <VBoxContainer>
      <Label text={ str(pick.call(3)) } />
    </VBoxContainer>
  )
}
